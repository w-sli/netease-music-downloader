# 安全说明

## 支持的版本

只有默认分支上的最新提交会被修复，请先确认问题在最新代码上仍存在。

## 设计边界（这些是有意为之，不是漏洞）

- 服务只绑定 `127.0.0.1`，且只接受回环地址作为接口来源；这是为了确保账号 Cookie 不会离开本机。
- 接口请求不跟随 HTTP 重定向：请求头带有账号 Cookie，跟随跳转可能把它发给其它主机。
- 音频与封面请求只发往接口返回的地址，且永远不带 Cookie、不使用系统代理。
- Cookie 保存在数据目录下的 `session.json`（POSIX 上权限 600），不会写入浏览器存储、不会出现在
  接口响应或导出的下载报告里。Windows 上该文件依赖用户配置目录的 ACL 保护。
- 下载目录由用户自行指定（可以是任意可写目录），这是产品行为；请不要把服务暴露给他人使用。

## 报告安全问题

优先使用 GitHub 的私密报告渠道：仓库页面 → **Security** → **Report a vulnerability**
（Security Advisory）。如果无法使用，请开一个不含细节的 Issue 说明需要私下联系。

报告里请包含：受影响版本、复现步骤、影响范围，以及你的判断依据。
**请不要**在 Issue、截图或日志里粘贴任何 Cookie、手机号、uid 或其它凭据。

## 我们不会做的事

- 不会要求你提供账号凭据来排查问题。
- 不会为了实现「解锁音质 / 绕过版权保护」而修改代码——这类请求会被直接关闭。
